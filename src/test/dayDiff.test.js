import { dayDiff } from '../client/js/api.js'

describe("lkfsldféka", () => {
    it("should .....", () => {
        const now = Date.now()
        const expected = 0
        const actual = dayDiff(now, now)
        expected(actual).toBe(expected)
    })
})